# Archivos temporales del sistema operativo.
# Cada proyecto (disponibilidad/, desempeno/) tiene ademas su propio
# .gitignore con lo especifico de su build (binarios, node_modules, etc.).
.DS_Store
Thumbs.db
